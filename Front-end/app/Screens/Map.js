import React from 'react';
import { SafeAreaView, MapView } from 'react-native';

function Map(props) {
    return (
        <SafeAreaView>
            <MapView/>
        </SafeAreaView>
    );
}
const styles = StyleSheet.create({
    
})
export default Map;